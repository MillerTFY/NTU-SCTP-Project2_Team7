[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "listenbrainz-dagster"
version = "0.1.0"
dependencies = [
    "dagster",
    "dagster-webserver",
]

[tool.dagster]
module_name = "listenbrainz_dagster"