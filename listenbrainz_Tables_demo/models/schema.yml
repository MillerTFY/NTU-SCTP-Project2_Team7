version: 2

models:
  - name: fact_listens
    description: "Fact table for listen events"
<<<<<<< HEAD
    tests:
=======

    # ── Table-level tests ───────────────────────────────────
    tests:
      # dbt-expectations: row count check
>>>>>>> refs/remotes/origin/main
      - dbt_expectations.expect_table_row_count_to_be_between:
          min_value: 100000000
          max_value: 200000000
          config:
            severity: error
            description: >
              fact_listens must contain 100M-200M rows.
=======

      # dbt_utils: business logic — listen_year must match
      # the year extracted from listened_at (derived column check)
      - dbt_utils.expression_is_true:
          arguments:
            expression: "listen_year = EXTRACT(YEAR FROM listened_at)"
            severity: error

      # dbt_utils: business logic — listen_month must be 1-12
      - dbt_utils.expression_is_true:
          arguments:
            expression: "listen_month BETWEEN 1 AND 12"
            severity: error

      # dbt_utils: business logic — listen_hour must be 0-23
      - dbt_utils.expression_is_true:
          arguments:
            expression: "listen_hour BETWEEN 0 AND 23"
            severity: error

>>>>>>> refs/remotes/origin/main
    columns:
      - name: listened_at
        tests:
          - not_null

      - name: user_name
        tests:
          - not_null
          - relationships:
              arguments:
                field: user_name
                to: ref('dim_user')

      - name: recording_msid
        tests:
          - not_null
          - relationships:
              arguments:
                field: recording_msid
                to: ref('dim_track')

      - name: artist_msid
        tests:
          - not_null
          - relationships:
              arguments:
                field: artist_msid
                to: ref('dim_artist')

      - name: release_msid
        tests:
          - not_null
          - relationships:
              arguments:
                field: release_msid
                to: ref('dim_release')

      - name: listen_hour
        tests:
=======
          # dbt-expectations: column range
>>>>>>> refs/remotes/origin/main
          - dbt_expectations.expect_column_values_to_be_between:
              min_value: 0
              max_value: 23
              config:
                severity: error
=======

          # dbt_utils: column type check (NEW from lesson)
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 23
                severity: warn
>>>>>>> refs/remotes/origin/main

      - name: time_of_day
        tests:
          - dbt_expectations.expect_column_values_to_be_in_set:
              value_set: ['Morning', 'Afternoon', 'Evening', 'Night']
              config:
                severity: error
=======
>>>>>>> refs/remotes/origin/main

      - name: listen_year
        tests:
          - dbt_expectations.expect_column_values_to_be_between:
              min_value: 2000
              max_value: 2018
              config:
                severity: warn
=======

          # dbt_utils: accepted_range equivalent (lesson pattern)
          - dbt_utils.accepted_range:
              arguments:
                min_value: 2000
                max_value: 2018
                severity: warn
>>>>>>> refs/remotes/origin/main
